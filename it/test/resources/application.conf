mongodb.uri = "mongodb://localhost:27017/low-earners-anomaly-frontend-integration"

pekko {
  log-dead-letters = 10
  log-dead-letters-during-shutdown = on
}

mongo-async-driver {
  pekko {

    loglevel = ERROR
  }
}
